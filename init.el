;; (add-to-list 'load-path "~/.emacs.d")

;; (setq debug-on-error t)
(load "~/.emacs.d/main_setup")

(setq ns-pop-up-frames nil)
(put 'downcase-region 'disabled nil)
(put 'narrow-to-region 'disabled nil)
